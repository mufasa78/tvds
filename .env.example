# Database Configuration
DATABASE_URL=postgresql://username:password@localhost/traffic_violations

# Flask Configuration
SESSION_SECRET=your-secret-key-for-session-management
FLASK_ENV=development
FLASK_DEBUG=1

# Upload Configuration
MAX_CONTENT_LENGTH=52428800  # 50MB in bytes

# Model Configuration
# Set to 'true' to use mock implementations if dependencies are missing
MOCK_DEPENDENCIES=false
